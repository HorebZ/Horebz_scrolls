{
  "tasks": {
    "dev": "deno run -A npm:vite dev",
    "build": "deno run -A npm:vite build",
    "preview": "deno run -A npm:vite preview",
    "check": "deno run -A npm:vite build --watch & deno run -A npm:svelte-check --tsconfig ./tsconfig.json"
  },
  "nodeModulesDir": "auto"
}
